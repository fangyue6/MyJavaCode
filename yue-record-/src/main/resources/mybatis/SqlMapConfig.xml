<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
		PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>


	<!-- 批量加载映射配置文件,mybatis自动扫描包下面的mapper接口进行加载 遵循一定的规范：需要将mapper接口类名和mapper.xml映射文件名称保持一致，且在一个目录中； 
		上边规范的前提是：使用的是mapper代理方法; -->
	<!-- <package name="com.record.mapper" /> -->

	<!-- 加载映射文件 -->
	<!--通过resource方法一次加载一个映射文件 -->
<!-- 	 <mappers> 
	<mapper resource="com/record/mapper/Student.xml"/> 
	<mapper resource="com/record/mapper/StudentMapper.xml"/> 
	</mappers> -->

	<!-- 通过mapper接口加载单个映射配置文件 遵循一定的规范：需要将mapper接口类名和mapper.xml映射文件名称保持一致，且在一个目录中； 
		上边规范的前提是：使用的是mapper代理方法; -->
<!--  	<mappers>
		<mapper class="com.record.mapper.AchievementMapper" />
		<mapper class="com.record.mapper.ContactMapper" />
		<mapper class="com.record.mapper.SchoolMapper" />
		<mapper class="com.record.mapper.StudentMapper" />
		<mapper class="com.record.mapper.TeacherMapper" />
	</mappers> -->
	
</configuration>

